#include "miLista.h"

int Lista_InsertarDespues(ListaEnlazada *lista, void *objeto, ElementoLista *elemento);

int Lista_InsertarDespues(ListaEnlazada *lista, void *objeto, ElementoLista *elemento)
{
	/*
  if ( (lista!=NULL) && (elemento!=NULL) && (lista->numeroElementos!=0) )
  {
  	int numeroElementos = lista->numeroElementos;
    ElementoLista *elem = (ElementoLista*)malloc(sizeof(ElementoLista));
    elem = Lista_Ultimo(lista);
    if(numeroElementos>0)
    {
      if(elem == elemento)
        Lista_InsertarFin(lista,elemento->objeto);
    }
    if(numeroElementos>1)
    {
      elem = Lista_Buscar(lista,objeto);
      if(elem != NULL)
      { 
      	elem
        if (primero == elemento)
        {
          return elemento->siguiente;
        }
        primero = primero->siguiente;
      }
    }

  }
  return -1;*/
	return 0;
}
